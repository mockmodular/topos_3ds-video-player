# 2026-04-02 vid_lifecycle.c 拆分完成

## 操作
- `source/video_player.c`（624行）→ 内容原封不动搬至 `source/vid_lifecycle.c`
- 删除 `source/video_player.c`
- Makefile 不需改动（wildcard 自动感知）

## 结果
`video_player.c` 彻底退场，`vid_lifecycle.c` 接管全部生命周期职责：
- 全局变量：`vid_player`、`vid_msg`
- 公开 API：`Vid_init` / `Vid_exit` / `Vid_resume` / `Vid_suspend` / `Vid_load_msg` / `Vid_query_init_flag` / `Vid_query_running_flag` / `Vid_get/set_use_hw_color_conversion`
- init 线程：`Vid_init_thread` / `Vid_exit_thread`
- 所有 `Vid_init_*` 数据函数

公开 API 声明仍在 `video_player.h`，调用方（main.c、sem.c 等）零改动。

## 拆分全流程完成
原 video_player.c 5135 行 → 8 个模块，video_player.c 彻底消失。
