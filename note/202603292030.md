# 2026-03-29 20:30

- 编译：`build.bat` 等价 `make clean && make` 已通过；顺带消除此前 GCC 报告的主要告警。
- `video_player.c`：`Vid_hid` 中 `sbs_swap_eyes_button` 原被错误地每帧清空（缩进误挂到 `MOVE_CONTENT` 的 `if`）；改为独立 `if` + 新增 `HID_SE0_SBS_SWAP_EYES_DESEL`；补全 `HID_SE0_SBS_SWAP_EYES_SEL` 按下高亮；seek 重开文件路径缓冲改为 `MAX_PATH_LENGTH + MAX_FILE_NAME_LENGTH` 消除 `snprintf` 截断告警。
- `menu.c`：`DEF_CURL_API_ENABLE` / `DEF_HTTPC_API_ENABLE` 缺省 0；仅在网络功能开启时定义 CURL/HTTP 相关宏，消除 `-Wundef` 与未使用宏；修正 `config.is_debug` 与 `Draw_screen_ready(BOTTOM)` 的缩进/从属关系。
- `decoder.c`：`Util_decoder_mvd_get_image` 删除未使用的 `cpy_size` 与 DMA 占位变量（当前路径用 `memcpy_asm`）。
