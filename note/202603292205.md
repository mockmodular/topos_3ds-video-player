# 2026-03-29 — vid_screen 模块拆分

- 新增 `include/vid_screen.h`、`source/vid_screen.c`。
- 从 `video_player.c` 移出：`Vid_fit_to_screen`、`Vid_change_video_size`、`Vid_enter_full_screen`、`Vid_exit_full_screen`、`Vid_increase_screen_brightness`、`Vid_decrease_screen_brightness`、`Vid_control_full_screen`。
- `video_player.c` 增加 `#include "vid_screen.h"`；Makefile 无需改（`source/*.c` 自动参与编译）。
- 本地 `make clean && make` 通过。
