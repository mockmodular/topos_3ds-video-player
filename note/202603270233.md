# 2026-03-27 — sem.c 第二轮精简（删除6个菜单）

## 改动内容

`source/system/sem.c` 从 **3052 行** → **1211 行**，削减约 60%。

### 删除的 6 个设置子菜单

| 菜单 | 功能 |
|------|------|
| MENU_UPDATE | 在线更新器 |
| MENU_LANGAGES | 多语言切换 |
| MENU_FONT | 扩展字体 |
| MENU_WIFI | WiFi 开关 |
| MENU_BATTERY | 省电模式 |
| MENU_RECORDING | 屏幕录制 |

### 每个菜单清除范围
- HID 触摸宏（`HID_*_SEL/CFM/DESEL`）
- Draw_image_data 静态变量（按钮图像）
- `Sem_draw_init` 内的初始化块
- `Sem_main` 内的绘制块
- `Sem_hid` 内的输入处理块
- 后台线程（record_thread / encode_thread / check_connectivity_thread / update_thread）及其 threadJoin/threadFree
- Watch_add / Watch_remove 调用
- 相关全局/静态变量

### 保留内容
- MENU_LCD / MENU_CONTROL / MENU_ADVANCED（3个菜单）
- Sem_hw_config_thread（亮度/省电/睡眠控制）
- 语言加载简化为默认英文（无语言选择UI）

### 关键设计决策

**MSG enum 保留原数字索引：**
```c
MSG_LCD      = 2,   // 原 sem_en.txt 中的索引
MSG_CONTROL  = 3,
MSG_ADVANCED = 6,
// ...
```
不连续，但与 romfs/gfx/msg/sem_en.txt 完全兼容，无需改资源文件。

**Sem_menu 重新从 0 编号：**
```c
MENU_LCD     = 0,
MENU_CONTROL = 1,
MENU_ADVANCED = 2,
MENU_MAX     = 3,
```

## 编译验证

sem.c 单文件编译（arm-none-eabi-gcc）：**零错误，零警告** ✅

完整 make（含中文路径 linker 问题）：预先已知的环境问题，非代码问题，不影响评估。

## 历史上下文

- 第一轮精简（同日）：删除 cam / curl / encoder / httpc / keyboard / mic / muxer 模块文件
- 精简目标：最终保留 video_player.c + menu.c + draw.c + decoder.c + converter.c + sem.c（仅LCD/CONTROL/ADVANCED）
