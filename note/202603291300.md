# 2026-03-29 改动记录

## 1. 修编译错误
- `source/system/sem.c` 加 `#include "system/util/cpu_usage.h"`
- 原因：sem.c 调用 `Util_cpu_usage_query_show_flag` / `Util_cpu_usage_set_show_flag` 但未 include 头文件

## 2. 修 sem 菜单全空白
- `romfs/gfx/msg/sem_en.txt` 补充 `<72>Show CPU usage</72>`
- 原因：`Util_parse_file` 严格要求 0~MSG_MAX-1 全部索引存在，缺一个整个数组加载失败

## 3. 删除非英文 sem 语言文件
- 删除：sem_de/es/hu/it/jp/pl/ro/ryu/zh-cn.txt
- 只保留 sem_en.txt

## 4. 修 CPU占用 ON 按钮无效
- `source/system/menu.c` Menu_init 中加 `Util_cpu_usage_init()` 调用
- 原因：`Util_cpu_usage_set_show_flag` 内部有 `if(!util_cpu_usage_init) return`，init 从未被调用

## 5. CPU占用显示移到下屏
- `source/system/menu.c`：从上屏渲染区移到下屏渲染区（`Draw_screen_ready(DRAW_SCREEN_BOTTOM,...)` 之后）
- `source/system/util/cpu_usage.c:152`：坐标改为 x=315, y=130（下屏右侧，中间偏下10px）
