# 2026-03-30 死代码宏清理（Step 5c+）

## 背景
Step 5c 完成后，`video_player.c` 开头约 166 行的 `HID_*_CFM` / `HID_*_UPDATE_RANGE` 宏失去全部调用方（CFM 执行段已替换为 enqueue→process 流程），触发约 50 条 `-Wunused-macros` 编译警告。

## 操作
Python 脚本按行号删除 61 行死代码宏定义（含部分孤立注释行）。

## 保留原因说明
| 保留宏 | 保留原因 |
|---|---|
| `HID_BRIGHTNESS_UP_PRE_CFM` | `Vid_hid()` auto_full_screen_count guard 仍调用 |
| `HID_BRIGHTNESS_DOWN_PRE_CFM` | 同上 |
| `HID_SCROLL_BAR_CFM` | Scroll 滚动段仍调用 |
| 全部 `_SEL` / `_DESEL` | SEL / DESEL 段仍调用 |

## 结果
编译 0 error、0 warning。`.3dsx` + `.cia` 正常生成。
